import MainContainer from './MainContainer/MainContainer';

export const App = () => {
  return (
    <div
      style={{
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
        color: '#010101',
      }}
    >
      <MainContainer />
    </div>
  );
};
